services:
  angular-app:
    image: nourlassoued/angularprojetservice:1.0.0
    container_name: angular-container
    ports:
      - "8081:80"  # Angular dispo sur http://localhost:8081
    networks:
      - backend

networks:
  backend:
    external: true